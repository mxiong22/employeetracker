const inquirer = require("inquirer")
const console_table =require("console.table")

const menu =() => {
    console.log("hello")
// ask user a prompt of choices, if chooses specific choice, they can use console.table to get information 
}
menu()